Dadas as representações em formato de string de dois números inteiros, retorne a representação em formato de string da soma desses números inteiros.

Por exemplo:

sumStrings('1','2') // => '3'
A representação em formato de string de um número inteiro não conterá caracteres além dos dez numerais "0" a "9".

Removi o uso de ` BigIntegerand` BigDecimalem Java.

Python: sua solução precisa funcionar com números enormes (cerca de um milhão de dígitos), converter para int não funcionará.